// Define your TestimonialItem interface (if not already present)
export interface TestimonialItem {
    id: string;
    author: string;
    role: string;
    company: string;
    quote: string;
    image: string;
    imageAlt: string;
  }